<Window x:Class="PL.Volunteer.VolunteerListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PL.Volunteer"
        Title="רשימת מתנדבים"
        Height="600" Width="900"
        Background="#E8F6FF"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Border CornerRadius="15" BorderBrush="#007ACC" BorderThickness="2"
            Background="White" Padding="20" Margin="20">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- סינון בצד שמאל -->
            <StackPanel Grid.Row="0" Grid.Column="0" VerticalAlignment="Top" Margin="0,0,15,0">
                <TextBlock Text="סינון לפי סוג קריאה:" FontSize="14" FontWeight="Bold"
                           Foreground="#004A7F" Margin="0,0,0,8"/>
                <ComboBox Width="130"
                          ItemsSource="{StaticResource CallTypesCollectionKey}"
                          SelectedItem="{Binding SelectedCallType, Mode=TwoWay}"
                          SelectionChanged="ComboBox_SelectionChanged"/>
             
            </StackPanel>

            <!-- מרכז: רשימה ומידע מתנדב -->
            <Grid Grid.Row="0" Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Text="מתנדבים ברשימה" FontSize="18" FontWeight="Bold"
                           Foreground="#007ACC" HorizontalAlignment="Center" Margin="0,0,0,8"/>
                <ListView x:Name="lsvCoursesList"
                          Grid.Row="1"
                          ItemsSource="{Binding VolunteerList}"
                          SelectedItem="{Binding SelectedVolunteer, Mode=TwoWay}"
                          MouseDoubleClick="lsvCoursesList_MouseDoubleClick">
                    <ListView.View>
                        <GridView>
                            <GridViewColumn Header="מספר" DisplayMemberBinding="{Binding Id}" Width="40"/>
                            <GridViewColumn Header="שם מלא" DisplayMemberBinding="{Binding FullName}" Width="120"/>
                            <GridViewColumn Header="טלפון" DisplayMemberBinding="{Binding Phone}" Width="100"/>
                            <GridViewColumn Header="סוג קריאה" DisplayMemberBinding="{Binding CurrentCallType}" Width="90"/>

                            <GridViewColumn Header="Handled" DisplayMemberBinding="{Binding TotalHandledCalls}" Width="70"/>
                            <GridViewColumn Header="Canceled" DisplayMemberBinding="{Binding TotalCanceledCalls}" Width="70"/>
                            <GridViewColumn Header="Expired" DisplayMemberBinding="{Binding TotalExpiredCalls}" Width="70"/>
                            <GridViewColumn Header="Current ID" DisplayMemberBinding="{Binding CurrentCallId}" Width="70"/>
                            <GridViewColumn Header="פעיל" DisplayMemberBinding="{Binding IsActive}" Width="60"/>

                            <!-- עמודת מחיקה -->
                            <GridViewColumn Header="מחיקה" Width="60">
                                <GridViewColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Content="✖"
                                                MinWidth="40"
                                                Padding="3"
                                                HorizontalAlignment="Center"
                                                Style="{StaticResource MainButtonStyle}"
                                                Click="DeleteButton_Click"
                                                Tag="{Binding Id}"/>
                                    </DataTemplate>
                                </GridViewColumn.CellTemplate>
                            </GridViewColumn>
                        </GridView>
                    </ListView.View>
                </ListView>
            </Grid>

            <!-- כפתורים בתחתית: חזרה והוספה -->
            <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="חזרה" Width="100" Height="35" Click="btnBack_Click"
                        Style="{StaticResource MainButtonStyle}" Margin="0,0,8,0"/>
                <Button Content="הוספה" Width="100" Height="35" Click="btnAdd_Click"
                        Style="{StaticResource MainButtonStyle}"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
