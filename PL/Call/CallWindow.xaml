<Window x:Class="PL.Call.CallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:PL.Call"
        Title="ניהול קריאה"
        Height="650" Width="750"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding RelativeSource={RelativeSource Self}}">

    <Window.Resources>
        <local:IdToVisibilityConverter x:Key="IdToVisibilityConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- פרטי קריאה -->
        <GroupBox Header="פרטי הקריאה" Grid.Row="0" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ת.ז. קריאה – יוצג רק במסך עריכה -->
                <TextBlock Text="ת.ז. קריאה:" Grid.Row="0" Grid.Column="0" Margin="5"
                           Visibility="{Binding Call.Id, Converter={StaticResource IdToVisibilityConverter}}"/>
                <TextBox Text="{Binding Call.Id}" Grid.Row="0" Grid.Column="1" IsEnabled="False" Margin="5"
                         Visibility="{Binding Call.Id, Converter={StaticResource IdToVisibilityConverter}}"/>

                <!-- תיאור -->
                <TextBlock Text="תיאור:" Grid.Row="1" Grid.Column="0" Margin="5"/>
                <TextBox x:Name="txtDescription" Text="{Binding Call.Description}" Grid.Row="1" Grid.Column="1" Margin="5"/>

                <!-- סוג -->
                <TextBlock Text="סוג קריאה:" Grid.Row="2" Grid.Column="0" Margin="5"/>
                <ComboBox x:Name="cmbCallType" SelectedItem="{Binding Call.Type}" Grid.Row="2" Grid.Column="1" Margin="5">
                    <ComboBox.ItemsSource>
                        <x:Array Type="{x:Type sys:String}" xmlns:sys="clr-namespace:System;assembly=mscorlib">
                            <sys:String>Medical</sys:String>
                            <sys:String>Emotional</sys:String>
                            <sys:String>Logistic</sys:String>
                        </x:Array>
                    </ComboBox.ItemsSource>
                </ComboBox>

                <!-- כתובת -->
                <TextBlock Text="כתובת:" Grid.Row="3" Grid.Column="0" Margin="5"/>
                <TextBox x:Name="txtAddress" Text="{Binding Call.Address}" Grid.Row="3" Grid.Column="1" Margin="5"/>

                <!-- זמן פתיחה -->
                <TextBlock Text="זמן פתיחה:" Grid.Row="4" Grid.Column="0" Margin="5"/>
                <TextBox Text="{Binding Call.OpenTime, StringFormat=G}" IsEnabled="False" Grid.Row="4" Grid.Column="1" Margin="5"/>

                <!-- זמן מקסימלי -->
                <TextBlock Text="מקס' לסיום (דקות):" Grid.Row="5" Grid.Column="0" Margin="5"/>
                <TextBox x:Name="dpMaxCompletionTime" Text="{Binding Call.MaxTimeToFinish}" Grid.Row="5" Grid.Column="1" Margin="5"/>
            </Grid>
        </GroupBox>

        <!-- כפתורים -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button x:Name="btnAddUpdate" Content="{Binding ButtonLabel}" Width="150" Margin="5" Click="btnAddUpdate_Click"/>
            <Button Content="סגור" Width="100" Margin="5" Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
