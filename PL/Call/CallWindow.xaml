<Window x:Class="PL.Call.CallWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:pl="clr-namespace:PL"
        xmlns:local="clr-namespace:PL.Call"
        Title="ניהול קריאה בודדת"
        Height="650" Width="750"
        WindowStartupLocation="CenterScreen"
        DataContext="{Binding Call, RelativeSource={RelativeSource Self}}">

    <Window.Resources>
        <local:IdToButtonTextConverter x:Key="IdToButtonTextConverter"/>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- טופס פרטי קריאה -->
        <GroupBox Header="פרטי הקריאה" Grid.Row="0" Margin="0,0,0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="150"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- ת.ז. קריאה -->
                <TextBlock Text="ת.ז. קריאה:" Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
                <TextBox Text="{Binding Id}" Grid.Row="0" Grid.Column="1" IsEnabled="False" Margin="5"/>

                <!-- תיאור -->
                <TextBlock Text="תיאור:" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
                <TextBox Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Grid.Column="1" Margin="5">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Open">
                                    <Setter Property="IsEnabled" Value="True"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="OpenAtRisk">
                                    <Setter Property="IsEnabled" Value="True"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- סוג קריאה -->
                <TextBlock Text="סוג קריאה:" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
                <ComboBox SelectedItem="{Binding Type, UpdateSourceTrigger=PropertyChanged}" Grid.Row="2" Grid.Column="1" Margin="5">
                    <ComboBox.ItemsSource>
                        <pl:CallTypesCollection/>
                    </ComboBox.ItemsSource>
                    <ComboBox.Style>
                        <Style TargetType="ComboBox">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Open">
                                    <Setter Property="IsEnabled" Value="True"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="OpenAtRisk">
                                    <Setter Property="IsEnabled" Value="True"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </ComboBox.Style>
                </ComboBox>

                <!-- כתובת -->
                <TextBlock Text="כתובת:" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
                <TextBox Text="{Binding Address, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Grid.Column="1" Margin="5">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Open">
                                    <Setter Property="IsEnabled" Value="True"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="OpenAtRisk">
                                    <Setter Property="IsEnabled" Value="True"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <!-- זמן פתיחה -->
                <TextBlock Text="זמן פתיחה:" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
                <TextBox Text="{Binding OpenTime, StringFormat=G}" Grid.Row="4" Grid.Column="1" IsEnabled="False" Margin="5"/>

                <!-- זמן מקסימום לסיום -->
                <TextBlock Text="זמן מקסימום לסיום (דק'):" Grid.Row="5" Grid.Column="0" VerticalAlignment="Center" Margin="5"/>
                <TextBox Text="{Binding MaxTimeToFinish, UpdateSourceTrigger=PropertyChanged}" Grid.Row="5" Grid.Column="1" Margin="5">
                    <TextBox.Style>
                        <Style TargetType="TextBox">
                            <Setter Property="IsEnabled" Value="False"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="InProgress">
                                    <Setter Property="IsEnabled" Value="True"/>
                                </DataTrigger>
                                <DataTrigger Binding="{Binding Status}" Value="InProgressAtRisk">
                                    <Setter Property="IsEnabled" Value="True"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

            </Grid>
        </GroupBox>

        <!-- היסטוריית הקצאות -->
        <GroupBox Header="היסטוריית הקצאות" Grid.Row="1" Margin="0,0,0,10">
            <DataGrid ItemsSource="{Binding Assignments}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      Margin="10"
                      Height="200">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="מתנדב" Binding="{Binding VolunteerName}" Width="200"/>
                    <DataGridTextColumn Header="זמן הקצאה" Binding="{Binding AssignedTime}" Width="150"/>
                    <DataGridTextColumn Header="סטטוס בהקצאה" Binding="{Binding AssignmentStatus}" Width="120"/>
                </DataGrid.Columns>
            </DataGrid>
        </GroupBox>

        <!-- לחצני פעולה -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10">
            <Button x:Name="btnAddUpdate"
                    Content="{Binding Id, Converter={StaticResource IdToButtonTextConverter}}"
                    Width="120" Margin="5"
                    Click="btnAddUpdate_Click"/>
            <Button x:Name="btnCancel"
                    Content="סגור"
                    Width="120" Margin="5"
                    Click="btnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
